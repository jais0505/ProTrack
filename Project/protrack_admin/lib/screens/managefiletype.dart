import 'package:flutter/material.dart';

class FileType extends StatelessWidget {
  const FileType({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Manage File Type'),
    );
  }
}
